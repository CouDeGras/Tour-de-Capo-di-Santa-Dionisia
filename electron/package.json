{
  "name": "capo-di-santa-dionisia",
  "productName": "Capo di Santa Dionisia",
  "version": "1.0.0",
  "description": "Irrigation dashboard and weather/MQTT service, bundled as a self-contained Linux AppImage.",
  "private": true,
  "main": "main.js",
  "scripts": {
    "stage": "bash build-backend.sh",
    "start": "electron .",
    "build": "npm run stage && electron-builder --linux AppImage"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.0"
  },
  "build": {
    "appId": "com.capo-di-santa-dionisia.dashboard",
    "productName": "Capo di Santa Dionisia",
    "artifactName": "${name}-${version}.${ext}",
    "files": [
      "main.js",
      "preload.js",
      "build/icon.png"
    ],
    "extraResources": [
      { "from": "resources/app", "to": "app" },
      { "from": "resources/venv", "to": "venv" }
    ],
    "linux": {
      "target": "AppImage",
      "icon": "build/icon.png",
      "category": "Utility"
    },
    "afterPack": "afterPack.js"
  }
}
